#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _ strlen: this function return length of the string
 * @s: input parametnr jojosej sjoe
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
